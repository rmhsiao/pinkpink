<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="m.mcoupledate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".ModifyMemorialDay" />
        <activity
            android:name=".HomePageActivity"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MemberData"
            android:label="@string/title_activity_member_data"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SiteInfoActivity" />
        <activity android:name=".ManageSiteActivity" />
        <activity android:name=".SiteRestaurantActivity" />
        <activity
            android:name=".SiteAttractionActivity"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddNewSiteActivity" />
        <activity android:name=".AddNewAttractionActivity" />
        <activity android:name=".AddNewRestaurantActivity" />
        <activity android:name=".MyAttractionActivity" />
        <activity android:name=".MyRestaurantActivity" />
        <activity
            android:name=".SiteActivity"
            android:label="@string/title_activity_site"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>